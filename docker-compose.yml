services:
  bookstore-db:
    image: postgres:17-alpine
    container_name: bookstore-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: bookstore
      POSTGRES_USER: bookstore
      POSTGRES_PASSWORD: bookstore_dev
    volumes:
      - bookstore-data:/var/lib/postgresql/data

  # Uncomment when the API project is added:
  # bookstore-api:
  #   build:
  #     context: .
  #     dockerfile: src/API/Dockerfile
  #   container_name: bookstore-api
  #   ports:
  #     - "5000:8080"
  #   environment:
  #     ASPNETCORE_ENVIRONMENT: Development
  #     ConnectionStrings__DefaultConnection: "Host=bookstore-db;Port=5432;Database=bookstore;Username=bookstore;Password=bookstore_dev"
  #   depends_on:
  #     - bookstore-db

volumes:
  bookstore-data:
